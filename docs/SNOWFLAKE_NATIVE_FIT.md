# Нативный Snowflake против описания вакансии

Что из меню Snowsight (Transformation, AI & ML, Horizon Catalog, Monitoring)
реально доступно в аккаунте `DX31435` и что из этого закрывает пункты
[job_description.ru.md](../job_description.ru.md).

Все строки ниже — проверка запросом, не документация вендора. Первая ревизия —
2026-08-12 (что доступно), вторая — 2026-08-13 (что сделано).

**Обозначения статуса:**

| | |
|---|---|
| ✅ | **сделано и проверено** — объект существует, замер приведён |
| 🟡 | **частично** — работает, но не в полном объёме; чего не хватает, написано рядом |
| ⬜ | **не начато** — с указанием, почему именно (не всегда «не успели») |

**Итог: ✅ 12 · 🟡 8 · ⬜ 9.** Всё, что помечено ⬜ в разделе 2, — не долг, а
решение, которое обязан принять владелец данных, а не скрипт.

---

## 1. Что доступно фактически — и что из этого теперь используется

| Возможность | Было 12.08 | Стало 13.08 | Статус |
|-------------|------------|-------------|--------|
| dbt Projects внутри Snowflake | тип есть, проектов **0** | `PLATA_RISK_DBT`, 1 модель, 10 тестов, 2 exposures, PASS=11 ERROR=0 | ✅ |
| Git-репозитории | есть, **0** | по-прежнему 0 — нужен `CREATE API INTEGRATION` под `ACCOUNTADMIN` | ⬜ |
| Dynamic tables | есть, **0** | `DT_COR_MONTHLY`, `TARGET_LAG = 1 day`, 45 строк | ✅ |
| Alerts | есть, **0** | `AL_RISK_GOV_WATCH`, состояние `started`, прогон дал 1 условие | ✅ |
| Cortex Agents | есть, **0** | `RISK_COPILOT`, 3 инструмента, 69 сессий | ✅ |
| Cortex Search | есть, **0** | `CS_RISK_DOCS`, 86 чанков, `ACTIVE/ACTIVE` | ✅ |
| MCP-сервер Snowflake | `HERMES_MCP.PUBLIC.CLAUDE_SQL` | им собрана вся песочница | ✅ |
| Функции Cortex (42 шт.) | доступны | используются `AI_EMBED` (внутри поиска), `SPLIT_TEXT_RECURSIVE_CHARACTER`, `DATA_AGENT_RUN` | 🟡 |
| LLM-инференс из региона | `llama3.1-8b`, `claude-4-sonnet` отвечают | оркестрация агента работает на них же | ✅ |
| Эмбеддинги и векторный поиск | работают | закрыты Cortex Search, руками не трогаем | ✅ |
| Семантическая вью | `SV_COR`, расхождение 0 | инструмент `cor_metrics` агента | ✅ |
| Системные DMF | доступны | без изменений с фазы 8 | 🟡 |
| AI Observability / Evaluations | не проверялось (нужен `ACCOUNTADMIN`) | не проверялось | ⬜ |
| Snowflake Postgres | не проверялось | не понадобился — вектор ушёл в Cortex Search | ⬜ |
| Horizon «Skills and plugins» | не проверялось | не проверялось | ⬜ |

---

## 2. Три параметра аккаунта, которые сами по себе являются governance-находкой

```sql
SHOW PARAMETERS LIKE 'CORTEX%' IN ACCOUNT;
```

| Параметр | Значение | Дефолт | Статус | Почему это важно |
|----------|----------|--------|--------|------------------|
| `CORTEX_ENABLED_CROSS_REGION` | `ANY_REGION` | `DISABLED` | ⬜ | Промпт с риск-данными может уехать на инференс **в любой регион**. Для финтеха под CNBV это решение о резидентности данных, а не настройка производительности |
| `CORTEX_MODELS_ALLOWLIST` | `ALL` | `ALL` | ⬜ | Какие модели допущены к риск-данным — не решено |
| `CORTEX_CODE_*_DAILY_EST_CREDIT_LIMIT_PER_USER` | `-1` | `-1` | ⬜ | Подушный лимит расхода на AI есть нативно и не включён |
| `CORTEX_CLIENT_READ_ONLY` | не задан на аккаунте | `false` | 🟡 | Выставляется **сессией сервиса** и принимается. На аккаунт не ставился |
| `CORTEX_MCP_SERVER_FORCE_SQL_EXEC_READ_ONLY` | `false` | `false` | ⬜ | Аккаунт-уровневый запрет записи для MCP поверх прав роли |

Строки ⬜ здесь помечены так намеренно. Готовые операторы лежат в
`sql/00b_bootstrap_copilot_accountadmin.sql` **закомментированными**: каждое
значение — решение владельца данных, а не действие скрипта. Осторожно с первым:
`DISABLED` в регионе `AWS_AP_SOUTH_1` может отрезать `claude-4-sonnet`, который
сейчас отвечает именно через cross-region.

Отдельная находка, всплывшая при внедрении: `CORTEX_CLIENT_READ_ONLY` разрешает
в сессии только `SELECT`, `EXPLAIN`, `SHOW` и `DESCRIBE`. Выставленный **первым**,
он глушит идущие следом `ALTER SESSION` — и таймаут, и `QUERY_TAG`. Ставить его
надо последним; порядок здесь не стиль, а условие работоспособности.

---

## 3. Соответствие пунктам вакансии

| Пункт JD | Нативный механизм | Статус | Что сделано / чего не хватает |
|----------|-------------------|--------|-------------------------------|
| «мы используем dbt в проде» | dbt Projects + Git repository | ✅ | Проект собран, `dbt tests` в DQ-контуре, exposures против реестра потребителей. Источник — стейдж, а не git |
| «SLA и observability по измерениям DMBoK» | Dynamic tables, DMF, Alerts + Tasks | ✅ | `TARGET_LAG` на витрине, alert идёт суточно по четырём источникам условий |
| «полнота метаданных и lineage» | Horizon Catalog: теги, `OBJECT_DEPENDENCIES` | 🟡 | Каталог и линидж с фазы 8. Владение — 14 объектов из 54, до Controlled не доведено |
| «контекстный слой для LLM, потребляемый автономно» | Cortex Analyst, Cortex Search, Cortex Agents | ✅ | Все три построены; агент отвечает 14.62% с контрактом и SQL |
| «что делает инфраструктуру пригодной для агентов» | MCP-сервер + read-only параметры | 🟡 | Агент ходит под `LLM_AGENT_RO`, аудит по `QUERY_TAG`. Аккаунт-уровневый запрет записи не включён |
| «регуляторные требования (CNBV, IFRS 9, Basel)» | параметры Cortex, `AI_REDACT`, маскирование | ⬜ | Три параметра в дефолте. Это ход №1 в разделе 5 |
| «документирование: Git-based, wiki или гибрид» | Git repository + `COMMENT` + реестры | 🟡 | Документация в git, реестры в базе. Связи между ними в Snowflake пока нет |
| «Airflow или аналогичные оркестраторы» (плюсом) | Tasks и графы задач | 🟡 | Alert работает по расписанию; графов задач нет — не понадобились |
| «переиспользуемые паттерны для новых стран» | git-источник сборки + теги + роли | ⬜ | Скрипты фаз воспроизводимы, но развёртывание под другой префикс не проверялось |

Про **AI Functions**, которых мы почти не касались:

| Функция | Применение | Статус |
|---------|------------|--------|
| `SPLIT_TEXT_RECURSIVE_CHARACTER` | нарезка приложений на 86 чанков | ✅ |
| `AI_EMBED_768` + `VECTOR_COSINE_SIMILARITY` | внутри Cortex Search | ✅ |
| `DATA_AGENT_RUN` | вызов агента из SQL и из сервиса | ✅ |
| `AI_PARSE_DOCUMENT` + `AI_EXTRACT` | приложения в карточки таблиц без ручной работы — ответ на «как масштабировать с 9 объектов до 40+» | ⬜ |
| `AI_REDACT` | вторая линия по PII в свободном тексте | ⬜ |
| `AI_FILTER` / `AI_CLASSIFY` | классификация 54 объектов по чувствительности | ⬜ |

---

## 4. Что это изменило в плане агента

| Решение | Статус | Итог |
|---------|--------|------|
| Поиск по документации — Cortex Search вместо pgvector на VPS | ✅ | Данные не покидают периметр, индекс обновляется сам, свой RRF не понадобился |
| Разрешение метрики — Cortex Analyst поверх `SV_COR` | ✅ | Фильтр `statement_num >= 2` внутри агрегата: разбавленный COR получить нельзя |
| Исполнение SQL — штатный механизм с read-only | 🟡 | Работает, но `sql_exec` агента фактически ограничен таблицей семантической вью — см. находку в стенограмме |
| Telegram и allowlist | ✅ | 28 прогонов, 23 разных вопроса, весь план проверки A–I |
| Проверка ответа граблями | ✅ | Линт по обязательным фильтрам карточек, прозу не токенизирует |
| Регресс-набор | 🟡 | `eval_agent` написан, 16 вопросов; baseline не снят — сравнивать пока не с чем |
| Трейсинг Langfuse | ⬜ | Env-переменные заведены, сервер не подключён |
| Учёт стоимости в USD | 🟡 | Считается запросом к `ORGANIZATION_USAGE`; в общий отчёт `hermes-memory` не сведён |

Оценка в 3.5–4 дня, о которой шла речь, подтвердилась: за один вечер закрыты
ходы 3–8 из раздела 5.

---

## 5. Порядок ходов — что закрыто

| # | Ход | Статус | Замер |
|---|-----|--------|-------|
| 1 | Три параметра Cortex привести в осознанное состояние | ⬜ | операторы готовы и закомментированы; решение за владельцем данных |
| 2 | `CREATE GIT REPOSITORY` + пересборка через `EXECUTE IMMEDIATE FROM` | ⬜ | нужен `CREATE API INTEGRATION` под `ACCOUNTADMIN` |
| 3 | dbt Project поверх фаз 5–6 | ✅ | PASS=11 ERROR=0 за 3.9 с; тест `not_null` нашёл расхождение канона и `SV_COR` |
| 4 | `ALERT` на дрейф и на нарушение SLA | ✅ | `started`, суточно; первый прогон — 1 условие, ложных 0 |
| 5 | Cortex Search по приложениям A–I | ✅ | 86 чанков; на опорном вопросе первой идёт карточка таблицы |
| 6 | `CREATE AGENT` с тремя инструментами | ✅ | Q01 14.62%, Q15 отказ по PII, Q16 редирект с дропнутой таблицы |
| 7 | Telegram-бот, allowlist, трейс, ночной eval | 🟡 | бот и allowlist работают; baseline регресса не снят |
| 8 | Dynamic table как образец декларативного SLA | ✅ | 45 строк, апрельский CC = 14.62 |

Дополнительно, сверх исходного списка:

| Ход | Статус | Замер |
|-----|--------|-------|
| Сервисный пользователь и закрытие разрыва «роль без носителя» | ✅ | `LLM_AGENT_RO.assigned_to_users` 0 → 1 |
| Разделение личностей: отвечающая и журнальная роли | ✅ | `COPILOT_WRITER` — 7 грантов, ни одного на витрины |
| Приёмка обоих контуров | ✅ | Snowflake 14/14 PASS, VPS PASS=17 FAIL=0 |
| Стенограмма прогона по приложениям A–I | ✅ | 23 вопроса: 11 PASS, 2 FAIL, 10 PARTIAL |
| Грант на `ODS.REF.HOLIDAYS` | ✅ | закрыл провал вопроса про рабочие дни; `USER_MGMT` намеренно не тронут |
| Network policy на IP VPS | ⬜ | ключ вне сервера пока остаётся рабочим |
| Бюджет на Cortex | ⬜ | `RM_COPILOT` покрывает 6.3 кредита из 16.1; главная статья не ограничена |

Первые четыре хода не требовали ни строчки кода вне Snowflake. Из девяти
пунктов вакансии полностью закрыты четыре, частично — четыре, не начат один.
