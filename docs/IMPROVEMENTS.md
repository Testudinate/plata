# Что улучшить в прототипе

Разбор состояния на 2026-08-12 и план развития. Три трека: довести сдачу,
поднять живого агента, усилить инженерную часть.

**Ревизия 2026-08-13: проставлены статусы.** Документ писался как план, теперь
показывает, что из него внедрено.

| | |
|---|---|
| ✅ | **внедрено и проверено** — с замером |
| 🟡 | **частично** — работает, чего не хватает написано рядом |
| ⬜ | **не сделано** — с причиной, не всегда «не успели» |

---

## 0. Где мы сейчас (замеры, а не оценки)

Проверено запросами к боевому аккаунту `DX31435` 2026-08-12, столбец справа —
что стало к 13-му:

| Что | Было | Стало |
|-----|------|-------|
| Утверждений о корректности | 26, провалов 0 | без изменений ✅ |
| Контекст-слой для агента | 9 карточек, 10 рёбер, 10 граблей, 17 терминов, 16 вопросов | плюс 86 чанков в поиске ✅ |
| Метрик под контрактом | 4 | без изменений |
| Реестр потребителей / владения | 10 / 14 связей | владение 29 записей, риск-слой 25 из 25 ✅ |
| Семантическая вью | расхождение с каноном 0 | стала инструментом агента ✅ |
| **Роль агента** | `LLM_AGENT_RO`, **пользователей 0** | **`assigned_to_users = 1`** ✅ |

Главный вывод разбора — последняя строка. Всё, что относится к Part 4, было
построено и проверено **кроме самого агента**: контекст-слой, права, золотой
набор, аудит — а субъекта, который под этой ролью ходит, не было. **Этот разрыв
закрыт 13 августа.**

---

## Трек A. Довести сдачу

| # | Задача | Статус | Что имеем |
|---|--------|--------|-----------|
| 1 | Английская версия ответа | ✅ | `answer/test-assignment-answer-en.{html,pdf}` на `main`, 70 КБ против 99 КБ русской — ближе к целевым 5–8 страницам |
| 2 | Ревизия приложений отдельным приложением | 🟡 | `source-review-en` есть; решение, выносить ли её в основной текст, не принято |
| 3 | Слайд-верхушка: три числа COR → мост → канон | ⬜ | вместо него сделана схема архитектуры `docs/architecture.{html,en.html}` |

Отдельно: ни русская, ни английская версия ответа **пока не содержат** того, что
построено 12–13 августа — агента, поиска, детектора дрейфа, находки с 14.94%.
Тексты писались, когда всего этого не существовало.

---

## Трек B. Plata Risk Copilot — докер на VPS + Telegram

Идея из вопроса: отдельный стек на VPS, доступ через Telegram, знания —
метаданные из git плюс данные в Snowflake. Она правильная, и вот почему сильнее,
чем кажется: это не «ещё одна демка», а **исполняемое доказательство Part 4**.

**Статус трека: собран и работает.** 28 прогонов из Telegram, 23 разных вопроса,
весь план проверки по приложениям A–I пройден.

### B.3 Что взято из существующих проектов, а не написано заново

Это ядро вопроса «что мы внедрили из наших проектов в Plata».

| Компонент | Откуда | Статус | Как легло |
|-----------|--------|--------|-----------|
| Паттерн FastAPI-сервиса, fail-closed конфиг | `hermes-memory/src/main.py`, дисциплина `.env` | ✅ | `app/api.py`, `app/config.py`: без обязательного секрета сервис не стартует, пустой allowlist — отказ стартовать |
| Трейсинг Langfuse 4.x с этапами и стоимостью | `hermes-memory/src/langfuse_trace.py`, `pricing.py` | ✅ | `app/tracing.py`, SDK 4.14 против сервера `langfuse/langfuse:4`. Трейсы доезжают, токены с разбивкой на кэш-чтение. Стоимость считает сам Langfuse — он знает тарифы `claude-opus-4-8`, свой `pricing.py` не понадобился |
| Правила мультистековой сети | скил `devops/docker-cross-stack-ops` | ✅ | сеть `llm-shared`, обращение по имени контейнера. Ровно та грабля и сработала: `127.0.0.1` внутри контейнера — своя петля, а `langfuse-web` оказался `langfuse-server` |
| Дисциплина «трейсинг не роняет запрос» | `hermes-memory/CLAUDE.md` | ✅ | все вызовы обёрнуты, `propagate_attributes` берётся через `getattr` |
| Алерты в Telegram | `hermes-memory/scripts/_alert.py` | ✅ | переработано: пороги считает Snowflake, очередь `DQ.ALERT_LOG`, бот доставляет и отмечает, кнопка «принял» |
| Eval-харнесс с baseline/compare/exit 2 | `hermes-memory/scripts/eval_retrieval.py`, `eval_weekly.py` | 🟡 | `app/eval_agent.py` написан, 16 вопросов, разбивка по difficulty; **baseline не снят** — сравнивать пока не с чем |
| Экспорт журнала в читаемый вид | практика стенограмм | ✅ | `app/export_log.py`, стенограмма 23 вопросов с вердиктами |
| Гибридный поиск (вектор + FTS, RRF, rerank), кэш | `hermes-memory/src/db.py`, `cache.py` | ⬜ | **сознательно не берём**: заменено Cortex Search внутри Snowflake. Данные не покидают периметр, индекс обновляется сам, свой RRF не выигрывает ничего кроме контроля |
| MCP-обвязка и OAuth-шим | `hermes-memory/mcp_server.py` | ⬜ | вторая поверхность для Claude Desktop не сделана; штатный MCP-сервер аккаунта при этом уже есть |
| Cron-контейнер, бэкапы, retention | `hermes-memory/docker-compose.yml` (`backup-cron`) | ⬜ | ночной eval запускается руками; журнал прогонов живёт в Snowflake и попадает под его бэкап |
| Детектор секретов | `hermes-memory/scripts/scan_secrets.sh` | ⬜ | к этому репозиторию не подключён; `.env` и ключи под `.gitignore`, проверяется скриптом приёмки |
| Готовый Telegram-шлюз Hermes Agent | Hermes Agent | ⬜ | написан свой бот на `aiogram` — нужен жёсткий allowlist по `chat_id` и своё меню команд |

Итог по заимствованиям: **✅ 6 · 🟡 1 · ⬜ 5**, и три из пяти незакрытых — это
решения «не берём», а не долг.

### B.4 Контракт безопасности

| Правило | Статус | Факт |
|---------|--------|------|
| Отдельный сервисный пользователь, key-pair, не пароль | ✅ | `PLATA_COPILOT_SVC`, ключ 400 и uid 10001, роль `LLM_AGENT_RO` |
| Свой warehouse со своим монитором | ✅ | `WH_COPILOT` + `RM_COPILOT`, квота 5 кредитов |
| Fail-closed по секретам | ✅ | проверяется `check_all.sh`, пять переменных |
| Только чтение — правами, а не промптом | ✅ | плюс `CORTEX_CLIENT_READ_ONLY` на сессии как второй слой |
| Allowlist `chat_id` в Telegram | ✅ | пустой список = сервис не стартует |
| `QUERY_TAG` связывает вопрос с человеком | ✅ | ставится на каждой сессии |
| Разделение личностей | ✅ | **сверх плана**: `COPILOT_WRITER` пишет журнал, риск-данных не видит |
| Network policy на IP VPS | ⬜ | ключ вне сервера пока остаётся рабочим |

### B.6 Порядок работ — факт против оценки

| Шаг | Оценка | Статус |
|-----|--------|--------|
| B1. Пользователь, ключи, warehouse, монитор | 0.5 дня | ✅ |
| B2. API: context_get, sql_run, metric_get, pitfall_check | 1.5 дня | ✅ линт по карточкам вместо pitfall_check |
| B3. Telegram-бот с allowlist | 0.5–1 день | ✅ |
| B4. pgvector с приложениями | 0.5 дня | ⬜ отменён в пользу Cortex Search |
| B5. Langfuse и учёт стоимости | 0.5 дня | 🟡 трейсинг есть, стоимость без ставки |
| B6. Расширение набора и ночной eval | 1 день | 🟡 набор есть, baseline нет |
| B7. MCP-поверхность | 0.5 дня | ⬜ |
| **Итого** | **5–5.5 дней** | **фактически один вечер** — за счёт отмены B4 и переноса тяжёлого в Snowflake |

---

## Трек C. Инженерные усиления песочницы

| # | Ход | Статус | Замер |
|---|-----|--------|-------|
| 1 | dbt поверх фаз 5–6 | ✅ | `PLATA_RISK_DBT`, PASS=11 ERROR=0; тест `not_null` нашёл расхождение канона и `SV_COR` на двух краевых месяцах |
| 2 | Детектор дрейфа, который реально работает | ✅ | `ALERT` в состоянии `started`; первая редакция срабатывала 33 раза из 33 и была переделана на инварианты знака |
| 3 | Git-репозиторий в Snowflake | ✅ | `PLATA_REPO` подключён к приватному репозиторию через PAT; dbt собирается из ветки `main`, PASS=11 |
| 4 | Streamlit in Snowflake | ⬜ | вместо него схема архитектуры в HTML |
| 5 | Генерация семантического YAML из реестра метрик | ⬜ | канон по-прежнему в двух местах, держится утверждением S13 и dbt-тестом |
| 6 | Владение до 100% на tier1–tier2 | ✅ | риск-слой покрыт целиком: 25 объектов из 25, без владельца ноль. Не имел владельца в том числе `V_COR_CANONICAL` |

---

## Приоритеты — что осталось

| Очередь | Что | Кто | Почему |
|---------|-----|-----|--------|
| 1 | Вписать в ответ то, что построено 12–13 августа | я | тексты отстают от стенда на два дня работы, а находки сильнее любого описания |
| 2 | Снять baseline регресса | вы, одна команда на VPS | без него `--compare` не с чем сравнивать |
| 3 | Одна сессия под `ACCOUNTADMIN`: network policy, бюджет на Cortex, read-only для MCP | вы | три пункта закрываются разом; git-интеграция уже сделана |
| 4 | Три параметра Cortex | решение владельца данных | резидентность инференса — не техническая настройка |
| 5 | MCP-поверхность, линидж, `TASK` для dbt | я | полировка |

Подробный разбор с исполнителями — в
[SNOWFLAKE_NATIVE_FIT.md, раздел 6](SNOWFLAKE_NATIVE_FIT.md#6-что-осталось-и-кто-это-закрывает).

## Что сознательно не делаем

- **Не даём агенту право записи** ни под каким видом, включая «временно для
  демонстрации». ✅ соблюдено
- **Не строим двусторонний синк git ↔ Snowflake.** ✅ соблюдено
- **Не тащим PII в контур агента.** ✅ соблюдено — грант на `ODS.REF.HOLIDAYS`
  выдан точечно, `ODS.USER_MGMT` не тронут
- **Не пишем свой веб-UI.** ✅ соблюдено
- **Не покупаем каталог.** ✅ соблюдено
