# snowflake-sandbox

Песочница в Snowflake, воспроизводящая ландшафт данных из `Test Assignment - Appendices`
(A–H): те же базы, схемы, таблицы, зерна (grain), соглашения о знаках и те же «грабли».

## Зачем

Тестовое задание (Parts 1–5) написано про данные, которых у нас нет. Ответ, в котором
три расчёта COR за апрель — это реальные три числа из реальных таблиц, а «канонический
метрик-слой», «DQ-мониторинг» и «контекст-слой для LLM» — работающие объекты, а не
слайд, отличается от текстового эссе на порядок. Песочница — фундамент под такой ответ.

## Документы

| Файл | Что внутри |
|------|------------|
| `PLAN.md` | Последовательный план создания объектов: фазы 0–10, зависимости, критерии приёмки |
| `INVENTORY.md` | Реестр объектов: что создаём, из какого приложения взято, что достоверно, а что домысливаем |

## Состояние

Песочница **собрана полностью**, все десять фаз закрыты.

| Замер | Значение |
|-------|----------|
| Объектов | 59 таблиц + 15 вью + 1 семантическая вью |
| Строк | 8 141 114 |
| Схем / баз | 23 / 10 |
| Хранилище | 0.307 ГБ |
| Кредитов на сборку | 2.67 (≈ 8 USD) |
| Утверждений о корректности | 26, все проходят |

Проверка состояния в одну строку:

```sql
SELECT * FROM RISK_GOV.DQ.BUILD_ASSERTIONS WHERE NOT passed;   -- пусто = всё в порядке
```

Три расчёта COR за апрель 2026 и мост между ними:

```sql
SELECT * FROM RISK_GOV.DQ.V_COR_RECONCILIATION WHERE month_start = '2026-04-01';
```

Выбран **вариант A** — настоящие базы, имена совпадают с приложениями.

| Шаг | Файл | Статус |
|-----|------|--------|
| Фаза 0 — гранты | `sql/00_bootstrap_accountadmin.sql` | **готово** — 10 баз, 5 ролей, монитор расходов |
| Фаза 1 — схемы и теги | `sql/01_schemas_and_tags.sql` | **готово** — 23 схемы по Appendix A, 9 тегов |
| Фаза 2 — календарь и справочники | `sql/02_calendar_and_reference.sql` | **готово** — HOLIDAYS, DIM_DATE, REF_PRODUCT, REF_SIGN_CONVENTION |
| Фаза 3 — структуры ODS | `sql/03_ods_structures.sql` | **готово** — ACCOUNT, USER_DATA, STATEMENTS, USDMXN |
| Фаза 4 — генератор портфеля | `sql/04_generator.sql` | **готово** — 1.5 млн строк панели, три расчёта COR расходятся |
| Фаза 5 — витрины DM и наполнение ODS | `sql/05_dm_marts.sql` | **готово** — 12 объектов, паттерны приложений отрабатывают |
| Фаза 6 — tableau, prov, скелеты | `sql/06_tableau_prov_skeletons.sql` | **готово** — 47 таблиц в 21 схеме, ~9.2 млн строк |
| Фаза 7 — проверочный контур | `sql/07_assertions.sql` | **готово** — 24 проверки, все проходят |
| Фаза 8 — governance-слой | `sql/08_governance.sql` | **готово** — каталог, канон и мост, DQ-скоркарта, реестры изменений, маскирование |
| Фаза 9 — контекст-слой для LLM | `sql/09_agent_context.sql` | **готово** — карточки, граф джойнов, грабли с детекторами, семантическая вью, 16 золотых вопросов |
| Фаза 10 — пересборка и снимок DDL | `sql/10_rebuild_and_teardown.sql` | **готово** — песочница собрана целиком |
| Фаза 11 — Cortex Search по документации | `sql/11_cortex_search.sql` | **готово** — 9 приложений, 86 чанков, одна поверхность на прозу и контракты |
| Фаза 12 — агент как объект аккаунта | `sql/12_agent.sql` | **готово** — 3 инструмента; Q01/Q15/Q16 отрабатывают |
| Фаза 13 — детектор дрейфа и SLA | `sql/13_alerts_and_dynamic.sql` | **готово** — alert идёт, dynamic table держит TARGET_LAG |
| Фаза 14 — dbt-проект внутри Snowflake | `sql/14_dbt_project.sql` | **готово** — PASS=11, ERROR=0 |
| Доступ из Telegram | `../agent-service/` | **написано, не развёрнуто** — нужен шаг 4 из `sql/00b_bootstrap_copilot_accountadmin.sql` |

Проверка корректности сборки в одну строку:

```sql
SELECT * FROM RISK_GOV.DQ.BUILD_ASSERTIONS WHERE NOT passed;   -- пусто = всё в порядке
```

Отступление от плана в фазе 3: наполнение `ACCOUNT`, `USER_DATA` и `STATEMENTS`
перенесено в фазу 4. Их строки обязаны происходить из той же сгенерированной
вселенной счетов, что и витрины DM, иначе ODS и DM разойдутся и джойны из
Appendix F перестанут сходиться. `USDMXN` наполнен сразу — курс от счетов
не зависит.

Проверено: дословный запрос из Appendix B («рабочие дни в месяце») отрабатывает
без правок и даёт 251 рабочий день в 2026 году, апрель — 20.

### Кто что запускает

Под `ACCOUNTADMIN` вручную запускаются **два** файла и только они:
`sql/00_bootstrap_accountadmin.sql` (базы, роли, монитор) и
`sql/00b_bootstrap_copilot_accountadmin.sql` (сервисный пользователь агента,
git-репозиторий, параметры Cortex, network policy) — второй после фаз 12–13,
потому что опирается на их объекты.

`sql/00_bootstrap_accountadmin.sql` — первый файл для ручного запуска,
и только под `ACCOUNTADMIN`: сессия MCP закреплена за `HERMES_MCP_ROLE`,
`USE ROLE` в ней запрещён, а `CREATE DATABASE`, `CREATE ROLE` и гранты на
аккаунте требуют админа.

Все остальные скрипты (`01`, `02`, дальше) выполняет рабочая сессия под
`HERMES_MCP_ROLE`. **Под `ACCOUNTADMIN` они упадут** с `Insufficient privileges
... must have CREATE SCHEMA granted on DATABASE ODS` — после фазы 0 базами
владеет `HERMES_MCP_ROLE`, и вместе с владением у админа ушло право создавать
в них схемы. Так и задумано: у объекта один владелец. При необходимости
`ACCOUNTADMIN` выдаёт себе доступ сам (`GRANT CREATE SCHEMA ON DATABASE ODS TO
ROLE ACCOUNTADMIN`) — запереть себя из аккаунта передачей владения нельзя.

## Целевой аккаунт

Проверено фактическими запросами 2026-08-12:

- аккаунт `DX31435`, регион `AWS_AP_SOUTH_1`, версия 10.28
- сессия MCP закреплена за ролью `HERMES_MCP_ROLE`, `USE ROLE` запрещён
- у роли есть: `CREATE SCHEMA` на `HERMES_MCP`, `USAGE`+`OPERATE` на `COMPUTE_WH`
- `CREATE DATABASE` на аккаунте — **нет**
- edition не ниже Enterprise: `CREATE TAG` проходит, системные DMF
  (`SNOWFLAKE.CORE.FRESHNESS`, `NULL_COUNT`, `DUPLICATE_COUNT`,
  `REFERENTIAL_INTEGRITY_COUNT`, `ROW_COUNT`, ...) доступны
- у роли есть `USAGE` на `CORTEX_AGENT_SERVER` — путь к семантическому слою для агента открыт
- пользователь `ANASTAS` имеет `ACCOUNTADMIN` и `ORGADMIN` — одноразовые гранты
  выдаются им вручную через Snowsight
